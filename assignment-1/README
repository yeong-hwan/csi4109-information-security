assignment-1
2019145019 장영환

I. Struct Fields

  House:
    *owner_name: char
    **keys: char
    *head: Node      // (Linked List for handle entered users)
  State:
    *key: char       // (latest inserted key)
    allowed: bool
    *key_head: Node  // (Linked List for handle rekey command)

  House는 초기 입력 정보 및 실내 인원 관리를,
  State는 최신 inserted key와 문 개방 정보 등 동적 정보를 관리합니다.

II. Code Structures
  - exception for argument usage
  
  - key setting

  - read each line until EOF and case division by token

  - insert
    - reset state about key & allowed.
    
  
  - turn
    - validation for turn key. (+fire fighter key exception handling)

  - enter
    - put user on list if state is allowed.
   